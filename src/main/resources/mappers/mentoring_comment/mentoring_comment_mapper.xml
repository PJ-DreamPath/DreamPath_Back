<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.korit.dreampath_back.mapper.MentoringCommentMapper">
    <resultMap id="mentoring_commentResultMap" type="com.korit.dreampath_back.entity.MentoringComment">
        <id property="commentId" column="comment_id" />
        <result property="userId" column="user_id" />
        <result property="postId" column="post_id" />
        <result property="content" column="content" />
        <result property="starPoint" column="star_point" />
        <result property="updateAt" column="update_at" />
        <result property="createdAt" column="created_at" />

        <result property="nickname" column="nickname" />
    </resultMap>

    <resultMap id="commentResultMap" type="com.korit.dreampath_back.entity.Mentoring">
        <result property="starPoint" column="star_point" />
        <result property="content" column="content" />
        <result property="createdAt" column="created_at" />

        <result property="nickName" column="nickname" />
    </resultMap>

    <insert id="createComment" useGeneratedKeys="true" keyProperty="commentId">
        insert into comment_tb(
        comment_id,
        user_id,
        post_id,
        content,
        star_point,
        update_at,
        created_at)

        values(
            default,
            #{userId},
            #{postId},
            #{content},
            #{starPoint},
            now(),
            now())

    </insert>

    <update id="updateComment">
        update comment_tb
        set
            content = #{content},
            star_point = #{starPoint},
            update_at = now()
        where
            comment_id = #{commentId}
    </update>


    <delete id="deleteComment">
        delete
        from
            comment_tb
        where
            comment_id = #{commentId}
    </delete>

    <select id="getCommentPageWithNickname" resultMap="commentResultMap">
        select

        ct.star_point,
        ct.content,
        ct.created_at,

        ut.nickname

        from
        comment_tb ct
        left outer join user_tb ut on(ct.user_id = ut.user_id)


        limit #{startIndex}, #{limitCount}

    </select>
    <select id="countTotalComments" resultType="java.lang.Integer">
        select
            count(*)
        from
            comment_tb
    </select>

    <select id="findByComment" resultMap="mentoring_commentResultMap">
        select
            comment_id,
            user_id,
            post_id,
            content,
            star_point,
            update_at,
            created_at
        from
            comment_tb
        where
            comment_id = #{commentId}
        and
            user_id = #{userId}
        and
            post_id = #{postId}
    </select>

</mapper>